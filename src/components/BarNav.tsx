import styles from '../styles/barNavstyles.module.css';

export const BarNav = () => {
    return (
        <header className={ styles.barNav }>
                <h1> Sudoku</h1>
        </header>
    )
}